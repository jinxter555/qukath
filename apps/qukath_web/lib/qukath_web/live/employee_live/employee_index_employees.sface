<div id={@update_id<>"s"} phx-update={@update_mode}>
  {#for employee <- @employees }
  <div id={@update_id <> "-" <> "#{employee.id}"}
       class={hide_deleted(employee, "container")}>
         <.func_attach employee={employee} func={@employee_attach}/>
    </div>
  {/for}
</div>

